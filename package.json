{
  "name": "lqs-uat-environment",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "build": "cd apps/client-ui && npm install && npm run build && cd ../.. && cp -r apps/client-ui/dist .",
    "deploy": "npm run build && wrangler pages deploy dist --project-name lqs-uat-environment"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.2",
    "axios": "^1.11.0",
    "csv-parser": "^3.2.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "wrangler": "^3.78.0"
  }
}
